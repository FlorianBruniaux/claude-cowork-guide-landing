---
import Layout from '../../layouts/Layout.astro'
import { WHITEPAPERS, TOTAL_PAGES, PDF_BASE_URL, API_URL } from '../../data/whitepapers-data.ts'

const AUTHOR = {
  '@type': 'Person',
  name: 'Florian BRUNIAUX',
  url: 'https://github.com/FlorianBruniaux',
}

const title = 'Whitepapers — 17 Guides | Claude Cowork'
const description = `17 whitepapers, ${TOTAL_PAGES}+ pages, PDF. From getting started to team deployment — practical guides for small and medium businesses. WP-00 free, no sign-up needed.`

const jsonLd = [
  {
    '@type': 'CollectionPage',
    name: 'Claude Cowork Whitepapers',
    description,
    url: 'https://cowork.bruniaux.com/whitepapers/',
    inLanguage: 'en',
    author: AUTHOR,
    hasPart: WHITEPAPERS.map(wp => ({
      '@type': 'Book',
      name: wp.titleEn,
      description: wp.subtitleEn,
      bookFormat: 'EBook',
      inLanguage: 'en',
      url: 'https://cowork.bruniaux.com/whitepapers/',
      author: AUTHOR,
      datePublished: wp.datePublished,
      numberOfPages: wp.pages,
      audience: {
        '@type': 'Audience',
        audienceType: wp.audienceEn,
      },
    })),
  },
]

const profiles = [
  {
    label: 'Getting started with Cowork',
    icon: '🚀',
    path: ['WP00', 'WP01', 'WP09'],
    desc: 'Entry point, effective prompts, then data security.',
  },
  {
    label: 'Tradesperson / Construction',
    icon: '🔨',
    path: ['WP00', 'WP03', 'WP06'],
    desc: 'Getting started, site quotes, then client prospecting.',
  },
  {
    label: 'Shop Owner / Retail',
    icon: '🏪',
    path: ['WP00', 'WP04', 'WP07'],
    desc: 'Getting started, commercial calendar, then communication.',
  },
  {
    label: 'Consultant / Freelancer',
    icon: '💼',
    path: ['WP05', 'WP06', 'WP08'],
    desc: 'Billable ROI, client prospecting, then organisation.',
  },
  {
    label: 'Executive deploying AI',
    icon: '👥',
    path: ['WP11', 'WP10', 'WP09'],
    desc: 'ROI first, team training, then data security.',
  },
  {
    label: 'Admin / Accounting',
    icon: '📊',
    path: ['WP02', 'WP08', 'WP01'],
    desc: 'Admin task automation, organisation, then prompts.',
  },
]
---

<Layout lang="en" title={title} description={description} jsonLd={jsonLd}>

  <!-- Hero -->
  <header class="page-hero">
    <div class="container">
      <div class="hero-label">Whitepapers & PDF Guides</div>
      <h1>Field guides, not overviews</h1>
      <p class="tagline">
        17 whitepapers covering every Claude Cowork use case for small and medium businesses.
        Concrete, structured, directly actionable.
      </p>
      <div class="hero-stats">
        <div class="stat">
          <span class="stat-num">17</span>
          <span class="stat-lbl">Whitepapers</span>
        </div>
        <div class="stat-sep" aria-hidden="true"></div>
        <div class="stat">
          <span class="stat-num">{TOTAL_PAGES}+</span>
          <span class="stat-lbl">Pages</span>
        </div>
        <div class="stat-sep" aria-hidden="true"></div>
        <div class="stat">
          <span class="stat-num">EN</span>
          <span class="stat-lbl">Language</span>
        </div>
        <div class="stat-sep" aria-hidden="true"></div>
        <div class="stat">
          <span class="stat-num">PDF</span>
          <span class="stat-lbl">Format</span>
        </div>
      </div>
      <div class="hero-cta">
        <a href="#table" class="btn btn-primary">Browse all guides</a>
        <a href="#free" class="btn btn-secondary">Free access</a>
      </div>
    </div>
  </header>

  <div class="main-content container">

    <!-- Open access featured card -->
    <section id="free" class="section">
      <h2 class="section-heading">No sign-up required</h2>
      <p class="section-sub">Direct download — no form needed.</p>
      <div class="featured-grid">
        {WHITEPAPERS.filter(wp => !wp.gated).map(wp => (
          <div class="featured-card">
            <div class="featured-header">
              <span class="wp-num-badge">WP-{wp.num}</span>
              <span class="badge badge-free">Free</span>
            </div>
            <h3 class="featured-title">{wp.titleEn}</h3>
            <p class="featured-sub">{wp.subtitleEn}</p>
            <div class="featured-meta">
              <span class="meta-pill">{wp.audienceEn}</span>
              <span class="meta-pill">~{wp.pages}p</span>
            </div>
            <div class="featured-actions">
              <a
                href={`${PDF_BASE_URL}/${wp.hashedFileEn ?? wp.hashedFileFr}`}
                target="_blank"
                rel="noopener"
                class="dl-btn dl-pdf"
                aria-label={`Download ${wp.titleEn} PDF`}
              >
                <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M21 15v4a2 2 0 01-2 2H5a2 2 0 01-2-2v-4M7 10l5 5 5-5M12 15V3" stroke-linecap="round" stroke-linejoin="round"/></svg>
                Download PDF
              </a>
            </div>
          </div>
        ))}
      </div>
    </section>

    <!-- Complete table -->
    <section id="table" class="section">
      <h2 class="section-heading">All 17 whitepapers</h2>
      <div class="wp-table">
        {WHITEPAPERS.map(wp => (
          <div class="wp-row" id={`wp-${wp.num}`}>
            <div class="wp-row-info">
              <div class="wp-row-header">
                <span class="wp-num-badge">WP-{wp.num}</span>
                {wp.badgeEn && (
                  <span class={`badge ${wp.badgeEn === 'Free' ? 'badge-free' : wp.badgeEn === 'Popular' ? 'badge-popular' : 'badge-new'}`}>
                    {wp.badgeEn}
                  </span>
                )}
                <span class="wp-audience">{wp.audienceEn}</span>
                <span class="wp-pages">~{wp.pages}p</span>
              </div>
              <h3 class="wp-row-title">{wp.titleEn}</h3>
              <p class="wp-row-subtitle">{wp.subtitleEn}</p>
              <div class="wp-tags">
                {wp.tags.slice(0, 3).map(tag => (
                  <span class="tag">{tag}</span>
                ))}
              </div>
            </div>
            <div class="wp-row-actions">
              {wp.gated ? (
                <div class="gated-block" data-wp={wp.num} data-slug={`cw-${wp.slugEn}.en`}>
                  <button
                    type="button"
                    class="get-btn"
                    data-wp={wp.num}
                    aria-expanded="false"
                    aria-controls={`form-${wp.num}`}
                  >
                    Get the guide
                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M19 9l-7 7-7-7" stroke-linecap="round" stroke-linejoin="round"/></svg>
                  </button>
                  <div id={`form-${wp.num}`} class="inline-form hidden" role="region" aria-label={`Get ${wp.titleEn}`}>
                    <form class="subscribe-form" data-wp={wp.num} data-slug={`cw-${wp.slugEn}.en`}>
                      <input type="text" name="website" class="honeypot" tabindex="-1" autocomplete="off" aria-hidden="true" />
                      <div class="form-row">
                        <input type="text" name="firstname" placeholder="First or last name" required class="form-input" autocomplete="name" />
                        <input type="email" name="email" placeholder="Email" required class="form-input" autocomplete="email" />
                      </div>
                      <button type="submit" class="submit-btn">
                        <span class="submit-text">Send to my inbox</span>
                        <span class="submit-spinner hidden" aria-hidden="true">···</span>
                      </button>
                      <p class="form-note">One email with the download link. No newsletter.</p>
                    </form>
                    <div class="form-success hidden" role="alert">
                      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M20 6L9 17l-5-5" stroke-linecap="round" stroke-linejoin="round"/></svg>
                      Check your inbox — link sent.
                    </div>
                    <div class="form-error hidden" role="alert">
                      Something went wrong. <a href="mailto:florian@bruniaux.com">Contact me</a> to receive the guide.
                    </div>
                  </div>
                </div>
              ) : (
                <div class="free-downloads">
                  <a href={`${PDF_BASE_URL}/${wp.hashedFileEn ?? wp.hashedFileFr}`} target="_blank" rel="noopener" class="dl-btn dl-pdf">
                    <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M21 15v4a2 2 0 01-2 2H5a2 2 0 01-2-2v-4M7 10l5 5 5-5M12 15V3" stroke-linecap="round" stroke-linejoin="round"/></svg>
                    Free PDF
                  </a>
                </div>
              )}
            </div>
          </div>
        ))}
      </div>
    </section>

    <!-- Reading paths -->
    <section id="parcours" class="section">
      <h2 class="section-heading">By profile</h2>
      <p class="section-sub">Start with what's relevant for you. Skip the rest.</p>
      <div class="paths-grid">
        {profiles.map(p => (
          <div class="path-card">
            <div class="path-icon" aria-hidden="true">{p.icon}</div>
            <h3 class="path-label">{p.label}</h3>
            <p class="path-desc">{p.desc}</p>
            <div class="path-sequence">
              {p.path.map((ref, i) => {
                const num = ref.replace('WP', '')
                const wp = WHITEPAPERS.find(w => w.num === num)
                return wp ? (
                  <span class="path-step">
                    {i > 0 && <span class="path-arrow" aria-hidden="true">→</span>}
                    <a href={`#wp-${wp.num}`} class="path-link">{ref}</a>
                  </span>
                ) : null
              })}
            </div>
          </div>
        ))}
      </div>
    </section>

    <!-- FAQ -->
    <section id="faq" class="section faq-section">
      <h2 class="section-heading">FAQ</h2>
      <div class="faq-grid">
        <details class="faq-item">
          <summary class="faq-q">What format are the whitepapers?</summary>
          <p class="faq-a">PDF only (rendered with Quarto+Typst). Optimised for screen reading and A4 printing.</p>
        </details>
        <details class="faq-item">
          <summary class="faq-q">Why do some require an email?</summary>
          <p class="faq-a">Not a paywall — everything is free. WP-00 is directly accessible without a form. The others ask for your email to send the download link and notify you when a revised version is released. One email, no newsletter unless you explicitly opt in.</p>
        </details>
        <details class="faq-item">
          <summary class="faq-q">How are these different from the main guide?</summary>
          <p class="faq-a">The guide (workflows, prompts, reference) is the comprehensive resource — dense and navigable. The whitepapers are structured narratives on a single topic: better for continuous reading, printing, or sharing with a colleague.</p>
        </details>
        <details class="faq-item">
          <summary class="faq-q">Will the guides be updated?</summary>
          <p class="faq-a">Yes. Each whitepaper has its own version number independent of the main guide. Your email link remains valid — it always points to the current version.</p>
        </details>
        <details class="faq-item">
          <summary class="faq-q">Are there French versions?</summary>
          <p class="faq-a">Yes — all 17 whitepapers are also available in French at the same address. The French editions include France-specific references (URSSAF, Infogreffe, Pappers) while the English editions adapt these for an international audience.</p>
        </details>
      </div>
    </section>

  </div>

  <div id="page-config" data-api-url={API_URL} hidden></div>

</Layout>

<style>
  /* Layout */
  .container { max-width: 1100px; margin: 0 auto; padding: 0 1.5rem; }
  .main-content { padding-bottom: 5rem; }

  /* Hero */
  .page-hero {
    padding: 4rem 0 3rem;
    text-align: center;
    border-bottom: 1px solid var(--border);
    background: var(--bg-primary);
  }
  .hero-label {
    display: inline-block;
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--accent);
    background: color-mix(in srgb, var(--accent) 12%, transparent);
    padding: 0.25rem 0.75rem;
    border-radius: 999px;
    margin-bottom: 1rem;
  }
  .page-hero h1 {
    font-size: clamp(2rem, 5vw, 3rem);
    font-weight: 800;
    color: var(--text-primary);
    margin: 0 0 0.75rem;
  }
  .tagline {
    font-size: 1.05rem;
    color: var(--text-secondary);
    max-width: 580px;
    margin: 0 auto 2rem;
    line-height: 1.65;
  }
  .hero-stats {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0;
    flex-wrap: wrap;
    margin-bottom: 2rem;
  }
  .stat { display: flex; flex-direction: column; align-items: center; padding: 0 1.5rem; }
  .stat-num { font-size: 1.4rem; font-weight: 800; color: var(--accent); line-height: 1.1; }
  .stat-lbl { font-size: 0.72rem; color: var(--text-muted); text-transform: uppercase; letter-spacing: 0.06em; margin-top: 0.15rem; }
  .stat-sep { width: 1px; height: 2rem; background: var(--border); }
  .hero-cta { display: flex; gap: 0.75rem; justify-content: center; flex-wrap: wrap; }
  .btn { display: inline-flex; align-items: center; padding: 0.625rem 1.25rem; border-radius: 0.5rem; font-size: 0.9rem; font-weight: 600; text-decoration: none; cursor: pointer; transition: all 0.2s; }
  .btn-primary { background: var(--accent); color: #fff; }
  .btn-primary:hover { background: var(--accent-hover); }
  .btn-secondary { border: 1px solid var(--border); color: var(--text-secondary); background: var(--bg-secondary); }
  .btn-secondary:hover { color: var(--text-primary); border-color: var(--accent); }

  /* Sections */
  .section { padding: 3rem 0 1rem; }
  .section-heading { font-size: 1.5rem; font-weight: 800; color: var(--text-primary); margin: 0 0 0.4rem; }
  .section-sub { font-size: 0.95rem; color: var(--text-secondary); margin: 0 0 1.75rem; }

  /* Badges */
  .badge {
    font-size: 0.65rem;
    font-weight: 700;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    padding: 0.15rem 0.5rem;
    border-radius: 3px;
  }
  .badge-free { background: color-mix(in srgb, #22c55e 20%, transparent); color: #16a34a; }
  .badge-popular { background: color-mix(in srgb, var(--accent) 20%, transparent); color: var(--accent); }
  .badge-new { background: color-mix(in srgb, #6db3f2 20%, transparent); color: #2563eb; }

  .wp-num-badge {
    font-family: var(--font-mono);
    font-size: 0.72rem;
    font-weight: 700;
    color: var(--accent);
    background: color-mix(in srgb, var(--accent) 10%, transparent);
    padding: 0.15rem 0.45rem;
    border-radius: 3px;
  }

  /* Featured cards (free WPs) */
  .featured-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(340px, 1fr)); gap: 1.25rem; margin-bottom: 0.5rem; }
  .featured-card {
    border: 1px solid var(--accent);
    border-radius: 0.75rem;
    padding: 1.5rem;
    background: color-mix(in srgb, var(--accent) 4%, var(--bg-secondary));
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }
  .featured-header { display: flex; align-items: center; gap: 0.5rem; }
  .featured-title { font-size: 1.1rem; font-weight: 700; color: var(--text-primary); margin: 0; }
  .featured-sub { font-size: 0.875rem; color: var(--text-secondary); margin: 0; line-height: 1.5; }
  .featured-meta { display: flex; gap: 0.5rem; flex-wrap: wrap; }
  .meta-pill { font-size: 0.72rem; color: var(--text-muted); border: 1px solid var(--border); padding: 0.1rem 0.4rem; border-radius: 999px; }
  .featured-actions { margin-top: 0.25rem; }

  /* Download buttons */
  .dl-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    font-size: 0.78rem;
    font-weight: 600;
    padding: 0.35rem 0.7rem;
    border-radius: 0.35rem;
    text-decoration: none;
    cursor: pointer;
    transition: all 0.15s;
    border: 1px solid var(--border);
    color: var(--text-secondary);
    background: var(--bg-secondary);
  }
  .dl-btn:hover { color: var(--accent); border-color: var(--accent); }

  /* Table */
  .wp-table { display: flex; flex-direction: column; border: 1px solid var(--border); border-radius: 0.75rem; overflow: hidden; }
  .wp-row {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 1.5rem;
    padding: 1.25rem 1.5rem;
    border-bottom: 1px solid var(--border);
    transition: background 0.15s;
  }
  .wp-row:last-child { border-bottom: none; }
  .wp-row:hover { background: var(--bg-secondary); }
  .wp-row-info { flex: 1; min-width: 0; }
  .wp-row-header { display: flex; align-items: center; gap: 0.5rem; margin-bottom: 0.35rem; flex-wrap: wrap; }
  .wp-audience { font-size: 0.72rem; color: var(--text-muted); font-style: italic; }
  .wp-pages { font-size: 0.7rem; color: var(--text-muted); font-family: var(--font-mono); }
  .wp-row-title { font-size: 1rem; font-weight: 700; color: var(--text-primary); margin: 0 0 0.25rem; }
  .wp-row-subtitle { font-size: 0.85rem; color: var(--text-secondary); margin: 0 0 0.5rem; line-height: 1.4; }
  .wp-tags { display: flex; gap: 0.35rem; flex-wrap: wrap; }
  .tag { font-size: 0.68rem; color: var(--text-muted); background: var(--bg-tertiary, var(--bg-secondary)); border: 1px solid var(--border); padding: 0.1rem 0.4rem; border-radius: 3px; font-family: var(--font-mono); }
  .wp-row-actions { flex-shrink: 0; display: flex; flex-direction: column; align-items: flex-end; gap: 0.5rem; min-width: 160px; }
  .free-downloads { display: flex; gap: 0.4rem; flex-wrap: wrap; justify-content: flex-end; }

  /* Get button */
  .get-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.5rem 1rem;
    border-radius: 0.45rem;
    background: var(--accent);
    color: #fff;
    font-size: 0.85rem;
    font-weight: 600;
    border: none;
    cursor: pointer;
    transition: background 0.15s;
    white-space: nowrap;
  }
  .get-btn:hover { background: var(--accent-hover); }
  .get-btn svg { transition: transform 0.2s; }
  .get-btn[aria-expanded="true"] svg { transform: rotate(180deg); }

  /* Inline form */
  .inline-form {
    margin-top: 0.75rem;
    padding: 1rem 1.25rem;
    background: var(--bg-secondary);
    border: 1px solid var(--border);
    border-radius: 0.5rem;
    width: 100%;
    max-width: 420px;
  }
  .inline-form.hidden { display: none; }
  .honeypot { position: absolute; width: 0; height: 0; opacity: 0; pointer-events: none; }
  .form-row { display: flex; gap: 0.5rem; margin-bottom: 0.75rem; }
  .form-input {
    flex: 1;
    padding: 0.45rem 0.7rem;
    border: 1px solid var(--border);
    border-radius: 0.375rem;
    font-size: 0.85rem;
    background: var(--bg-primary);
    color: var(--text-primary);
    min-width: 0;
    transition: border-color 0.15s;
  }
  .form-input:focus { outline: none; border-color: var(--accent); }
  .submit-btn {
    width: 100%;
    padding: 0.55rem 1rem;
    background: var(--accent);
    color: #fff;
    border: none;
    border-radius: 0.4rem;
    font-size: 0.875rem;
    font-weight: 600;
    cursor: pointer;
    transition: background 0.15s;
    margin-bottom: 0.5rem;
  }
  .submit-btn:hover { background: var(--accent-hover); }
  .submit-btn:disabled { opacity: 0.6; cursor: not-allowed; }
  .form-note { font-size: 0.72rem; color: var(--text-muted); text-align: center; margin: 0; }
  .form-success, .form-error {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.6rem 0.75rem;
    border-radius: 0.4rem;
    font-size: 0.85rem;
    font-weight: 500;
  }
  .form-success.hidden, .form-error.hidden { display: none; }
  .form-success { background: color-mix(in srgb, #22c55e 12%, transparent); color: #15803d; }
  .form-error { background: color-mix(in srgb, #ef4444 12%, transparent); color: #dc2626; }
  .form-error a { color: inherit; text-decoration: underline; }
  .submit-spinner { font-family: monospace; letter-spacing: 0.2em; }

  /* Reading paths */
  .paths-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 1rem; }
  .path-card { padding: 1.25rem; border: 1px solid var(--border); border-radius: 0.625rem; background: var(--bg-secondary); display: flex; flex-direction: column; gap: 0.5rem; }
  .path-icon { font-size: 1.4rem; line-height: 1; }
  .path-label { font-size: 0.95rem; font-weight: 700; color: var(--text-primary); margin: 0; }
  .path-desc { font-size: 0.825rem; color: var(--text-secondary); margin: 0; line-height: 1.45; }
  .path-sequence { display: flex; align-items: center; gap: 0.25rem; flex-wrap: wrap; margin-top: 0.25rem; }
  .path-step { display: flex; align-items: center; gap: 0.25rem; }
  .path-arrow { color: var(--text-muted); font-size: 0.8rem; }
  .path-link {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    font-weight: 700;
    color: var(--accent);
    text-decoration: none;
    background: color-mix(in srgb, var(--accent) 10%, transparent);
    padding: 0.15rem 0.4rem;
    border-radius: 3px;
    transition: background 0.15s;
  }
  .path-link:hover { background: color-mix(in srgb, var(--accent) 20%, transparent); }

  /* FAQ */
  .faq-section { padding-bottom: 2rem; }
  .faq-grid { display: flex; flex-direction: column; border: 1px solid var(--border); border-radius: 0.625rem; overflow: hidden; }
  .faq-item { border-bottom: 1px solid var(--border); }
  .faq-item:last-child { border-bottom: none; }
  .faq-q {
    list-style: none;
    padding: 1rem 1.25rem;
    font-weight: 600;
    font-size: 0.95rem;
    color: var(--text-primary);
    cursor: pointer;
    user-select: none;
    display: flex;
    justify-content: space-between;
    align-items: center;
    transition: background 0.15s;
  }
  .faq-q:hover { background: var(--bg-secondary); }
  .faq-q::after { content: '+'; font-size: 1.2rem; color: var(--accent); flex-shrink: 0; }
  .faq-item[open] .faq-q::after { content: '-'; }
  .faq-a { padding: 0 1.25rem 1rem; font-size: 0.9rem; color: var(--text-secondary); line-height: 1.6; margin: 0; }

  /* Responsive */
  @media (max-width: 768px) {
    .hero-stats { gap: 0; }
    .stat { padding: 0 1rem; }
    .stat-num { font-size: 1.2rem; }
    .wp-row { flex-direction: column; gap: 1rem; }
    .wp-row-actions { align-items: flex-start; min-width: auto; width: 100%; }
    .inline-form { max-width: 100%; }
    .form-row { flex-direction: column; }
    .featured-grid { grid-template-columns: 1fr; }
  }
  @media (max-width: 480px) {
    .stat-sep { display: none; }
    .stat { padding: 0.5rem 0.75rem; }
  }
</style>

<script>
  const apiUrl = (document.getElementById('page-config') as HTMLElement | null)?.dataset.apiUrl ?? 'https://florian.bruniaux.com/api/subscribe'
  // Restore saved contact details into a form
  function prefillContactFields(form: Element) {
    try {
      const name = localStorage.getItem('cowork-contact-name')
      const email = localStorage.getItem('cowork-contact-email')
      if (name) (form.querySelector('[name="firstname"]') as HTMLInputElement).value = name
      if (email) (form.querySelector('[name="email"]') as HTMLInputElement).value = email
    } catch {}
  }

  // Toggle inline form
  document.querySelectorAll<HTMLButtonElement>('.get-btn').forEach(btn => {
    btn.addEventListener('click', () => {
      const wpNum = btn.dataset.wp
      const form = document.getElementById(`form-${wpNum}`)
      if (!form) return
      const isOpen = btn.getAttribute('aria-expanded') === 'true'
      btn.setAttribute('aria-expanded', isOpen ? 'false' : 'true')
      form.classList.toggle('hidden', isOpen)
      if (!isOpen) {
        prefillContactFields(form)
        const nameInput = form.querySelector<HTMLInputElement>('[name="firstname"]')
        ;(nameInput?.value ? form.querySelector<HTMLInputElement>('[name="email"]') : nameInput)?.focus()
      }
    })
  })

  // Capture UTM params
  const urlParams = new URLSearchParams(window.location.search)
  const utmSource = urlParams.get('utm_source') || document.referrer || 'direct'
  const utmCampaign = urlParams.get('utm_campaign') || null

  // Handle form submission
  document.querySelectorAll<HTMLFormElement>('.subscribe-form').forEach(form => {
    form.addEventListener('submit', async (e) => {
      e.preventDefault()
      const wpNum = form.dataset.wp
      const slug = form.dataset.slug!  // e.g. "cw-01-prompts-efficaces-non-devs.fr"

      const honeypot = (form.querySelector('[name="website"]') as HTMLInputElement)?.value
      if (honeypot) return

      const email = (form.querySelector('[name="email"]') as HTMLInputElement)?.value
      const firstName = (form.querySelector('[name="firstname"]') as HTMLInputElement)?.value

      // Persist contact details
      try {
        if (firstName) localStorage.setItem('cowork-contact-name', firstName)
        if (email) localStorage.setItem('cowork-contact-email', email)
      } catch {}

      const submitBtn = form.querySelector<HTMLButtonElement>('.submit-btn')
      const submitText = form.querySelector('.submit-text')
      const submitSpinner = form.querySelector('.submit-spinner')
      const successMsg = form.parentElement?.querySelector('.form-success')

      if (submitBtn) submitBtn.disabled = true
      submitText?.classList.add('hidden')
      submitSpinner?.classList.remove('hidden')

      try {
        const res = await fetch(apiUrl, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            email,
            firstName,
            filename: slug,
            website: '',
            source: utmSource,
            ...(utmCampaign && { utmCampaign }),
          }),
        })

        if (!res.ok) throw new Error(`API ${res.status}`)

        form.classList.add('hidden')
        successMsg?.classList.remove('hidden')
        try { localStorage.setItem('cowork-subscriber', '1') } catch {}
        if (typeof (window as any).gtag === 'function') {
          (window as any).gtag('event', 'ebook_subscribe', {
            guide: slug,
            format: 'pdf',
            source: utmSource,
            ...(utmCampaign && { campaign: utmCampaign }),
          })
        }
      } catch (err) {
        form.classList.add('hidden')
        const errorMsg = form.parentElement?.querySelector('.form-error')
        errorMsg?.classList.remove('hidden')
        console.warn('Subscribe API error:', err)
      } finally {
        if (submitBtn) submitBtn.disabled = false
        submitText?.classList.remove('hidden')
        submitSpinner?.classList.add('hidden')
      }
    })
  })
</script>
